<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Your Memo's</title>
    <link rel="stylesheet" href="stylesheets/canvas.css">
  </head>
  <body>
    <div class="" id="container">
      <h2 align='center'>Write Your Memo's</h2>
      <hr>
      <form class="" action="/users/create" method="post">
        <table style="width:50%" align="center">
          <thead>
            <tr>
              <td>
                TITLE:<br>
                <textarea name="title" rows="5" cols="70"></textarea>
              </td>
              <tr>
                <td>
                  <select class="" name="is_complete">
                    <option value="" disabled selected="">Choose status</option>
                    <option value="uncomplete">uncomplete</option>
                    <option value="complete">complete</option>
                  </select>
                  <select name='id_user' required>
                   <option value='' disabled selected>Choose email</option>
                   <%
                   users.forEach(value => {
                   %>
                   <option value='<%= value.id %>'><%= value.email %></option>
                   <%
                   });
                   %>
                 </select>
                 <button type="submit" name="add">Submit</button>
                </td>
              </tr>
            </tr>
          </thead>
          <tbody>
          </tbody>
        </table>
      </form>
    </div>
  </body>
</html>